<!-- Slide 31 -->

<div class="text-sm font-mono tracking-widest text-cyan-600 uppercase mb-2">08 · Broker & CI/CD</div>

# Broker & Deployment Gate<sup>1</sup>

```mermaid {scale: 0.50}
flowchart TB
  subgraph Lifecycle["Contract lifecycle"]
    direction LR
    CT[Consumer tests] --> PF[Pact file]
    PF -->|publish| PB[(Pact Broker)]
    PB -->|fetch| PV[Provider verification]
    PV -->|result| MX[(Compatibility matrix)]
  end

  subgraph Delivery["Deployment gate"]
    direction LR
    MX --> CID{can-i-deploy?}
    CID -->|compatible| DEP[Deploy independently]
    CID -->|unknown / failed| STOP[Stop pipeline]
  end
```

<div v-click class="mt-2 p-2 bg-cyan-50 border border-cyan-200 rounded-lg text-sm text-center">
  Broker liên kết <strong>version Consumer</strong> + <strong>version Provider</strong> + <strong>kết quả verification</strong> → Compatibility matrix
</div>

<div class="footnotes">
<div class="fn-item"><sup>1</sup><strong>Deployment gate</strong> — Điểm kiểm soát quyết định có được deploy hay không.</div>
</div>

<!--
Broker = registry + compatibility matrix.
-->